# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、测试用例清单

### 3.1 问答核心 API 测试

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|----------|--------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId、response_time_ms、answer_source | REQ-002 | P0 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-002 | P0 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-002 | P0 |
| TC-M01-004 | Integration | `test_ask_demo_fallback` | 无 API Key → `answer_source='demo'`, `llm_used=false` | REQ-002 | P0 |
| TC-M01-005 | Integration | `test_capabilities` | `GET /capabilities` → 200, 含 copaw_configured(bool)、bailian_configured(bool) | REQ-006 | P1 |
| TC-M01-006 | Contract | `test_ask_llm_used_is_bool` | `llm_used` 字段类型为 bool，非字符串 | 规则-005 | P0 |
| TC-M01-007 | Contract | `test_ask_answer_source_enum` | `answer_source` ∈ {copaw, bailian, demo} | 规则-006 | P0 |

### 3.2 会话管理 API 测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | `GET /sessions` → 200, 返回 sessions 数组，每项含 session_id、title、created_at、query_count | REQ-001 | P0 |
| TC-M01-021 | Integration | `POST /sessions` → 201, 返回含 session_id(UUID)、title("新会话")、query_count(0) | REQ-001 | P0 |
| TC-M01-022 | Integration | `POST /sessions` 自定义 title → 201, title 为传入值 | REQ-001 | P1 |
| TC-M01-023 | Integration | `DELETE /sessions/<id>` → 200, 返回 message + deleted_records | REQ-001 | P0 |
| TC-M01-024 | Integration | `DELETE /sessions/<不存在id>` → 404, `SESSION_NOT_FOUND` | REQ-001 | P0 |
| TC-M01-025 | Integration | 删除会话后 `GET /sessions/<id>/records` 返回空或 404 | REQ-001 | P0 |

### 3.3 问答记录 API 测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | `GET /sessions/<id>/records` → 200, 有记录时返回按时间正序的 records 数组 | REQ-003 | P0 |
| TC-M01-031 | Integration | `GET /sessions/<id>/records` → 200, 无记录时返回空数组 `[]` | REQ-003 | P1 |
| TC-M01-032 | Integration | `GET /sessions/<不存在id>/records` → 404, `SESSION_NOT_FOUND` | REQ-003 | P0 |

### 3.4 观点分析测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-050 | Integration | 观点整合请求返回包含共同观点和差异观点的结构化回答 | REQ-004 | P0 |
| TC-M01-051 | Integration | 观点整合结果中标注来源研报名称 | REQ-004 | P0 |
| TC-M01-052 | Integration | 无研报数据时返回合理提示而非报错 | REQ-004 | P1 |
| TC-M01-055 | Integration | 观点回测请求返回关联的大盘指标数据 | REQ-005 | P0 |
| TC-M01-056 | Integration | 回测结果标注数据来源和时间范围 | REQ-005 | P0 |
| TC-M01-057 | Integration | 无大盘数据时 Demo 模式兜底返回 | REQ-005 | P1 |

### 3.5 Storage 层单元测试

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | Unit | `Storage.__init__` | 数据目录不存在时自动创建，JSON 文件初始化为空数组 | P0 |
| TC-M01-041 | Unit | `create_session` | 创建后 get_sessions 可找到，query_count=0 | P0 |
| TC-M01-042 | Unit | `get_sessions` | 返回全部会话，按 created_at 倒序 | P0 |
| TC-M01-043 | Unit | `delete_session` | 删除后 get_sessions 不再包含，且关联 records 被级联删除 | P0 |
| TC-M01-044 | Unit | `add_record` | 写入后 get_records_by_session 可找到，session.query_count +1 | P0 |
| TC-M01-045 | Unit | `get_records_by_session` | 按 session_id 过滤正确，按 timestamp 正序 | P0 |
| TC-M01-046 | Unit | `update_session` | 更新 title 后 get_sessions 返回新 title | P1 |
| TC-M01-047 | Unit | `delete_records_by_session` | 删除后 get_records_by_session 返回空列表 | P0 |
| TC-M01-048 | Unit | 首次问答自动命名 | query_count 从 0→1 时 title 自动改为 query 前 20 字 + `...` | P0 |

### 3.6 安全测试

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-060 | Integration | Prompt 注入：输入含特殊指令的 query，系统不执行指令 | 11 §5 | P1 |
| TC-M01-061 | Unit | API Key 不在日志中输出 | 11 §3 | P1 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过 | **是** |
| **G-UNIT** | Unit 测试全绿 | **是** |
| **G-INT** | Integration 测试全绿 | **是** |
| **G-AC** | P0 验收标准全覆盖 | **是** |
| **G-PERF** | `response_time_ms < 5000` | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-3/tianjiazhou/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 观点整合 / 回测验证 | **高** | **强烈建议 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC |
|----------|-----------|-----------|
| REQ-001 | TC-M01-020~025 | — |
| REQ-002 | TC-M01-001~004, 006, 007 | — |
| REQ-003 | TC-M01-030~032 | — |
| REQ-004 | TC-M01-050~052 | — |
| REQ-005 | TC-M01-055~057 | — |
| REQ-006 | TC-M01-005 | — |
| 规则-005 | TC-M01-006 | — |
| Storage | TC-M01-040~048 | — |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
